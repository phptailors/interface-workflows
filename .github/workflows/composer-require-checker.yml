---
name: Composer Require Checker

on:
  - workflow_call
  - pull_request
  - push

jobs:
  composer_require_checker:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
        php-version:
          - "8.0"
          - "8.1"
          - "8.2"
          - "8.3"
          - "8.4"
          - "8.5"
        dependencies:
          - lowest
          - highest

    name: ${{ matrix.os }}:PHP-${{ matrix.php-version }}:${{ matrix.dependencies }}
    steps:
      - uses: actions/checkout@v6
      - uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php-version }}

      - run: composer --version

      - if: matrix.dependencies == 'highest'
        run: composer update --no-interaction --no-progress --ansi

      - if: matrix.dependencies == 'lowest'
        run: composer update --no-interaction --no-progress --ansi --prefer-lowest

      - run: composer bin composer-require-checker update --no-progress --ansi

      - if: ${{ github.event_name == 'workflow_call' }}
        name: Run composer-require-checker
        run: vendor/bin/phptailors-workflows run:composer-require-checker

      - if: ${{ github.event_name != 'workflow_call' }}
        name: Run composer-require-checker
        run: php bin/phptailors-workflows run:composer-require-checker
