{
    "name": "phptailors/workflows",
    "description": "Reusable Workflows",
    "type": "library",
    "authors": [
        {
            "name": "Paweł Tomulik",
            "email": "pawel@tomulik.pl"
        }
    ],
    "support": {
        "issues": "https://github.com/phptailors/workflows/issues",
        "source": "https://github.com/phptailors/workflows"
    },
    "require": {
        "php": ">=8.0",
        "symfony/console": ">= 5.0",
        "symfony/process": ">= 5.0",
        "symfony/property-access": ">= 5.0",
        "composer/composer": "^2.0"
    },
    "require-dev": {
        "bamarni/composer-bin-plugin": "^1.8"
    },
    "autoload": {
        "psr-4": {
            "Tailors\\Console\\Workflows\\": "src/"
        },
        "exclude-from-classmap": [
            "/tests/"
        ]
    },
    "autoload-dev": {
        "psr-4": {
            "Tailors\\Console\\Workflows\\": "tests/src/"
        }
    },
    "bin": [
        "bin/phptailors-workflows"
    ],
    "extra": {
        "bamarni-bin": {
            "bin-links": false,
            "forward-command": false,
            "target-directory": "vendor-bin"
        },
        "branch-alias": {
            "dev-master": "1.0-dev"
        }
    },
    "config": {
        "allow-plugins": {
            "bamarni/composer-bin-plugin": true
        }
    },
    "scripts": {
        "post-install-cmd": [
            "(mkdir -p vendor/bin && cd vendor/bin && ([ -e phptailors-workflows ] || ln -s ../../bin/phptailors-workflows))"
        ],
        "post-update-cmd": [
            "(mkdir -p vendor/bin && cd vendor/bin && ([ -e phptailors-workflows ] || ln -s ../../bin/phptailors-workflows))"
        ]
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
